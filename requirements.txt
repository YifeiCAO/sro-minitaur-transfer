# Core -- diagnostics half (Phase 0b/0c). CPU only.
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
pyyaml>=6.0

# Model half (Phase 1-4) -- install on a GPU box / Colab:
#   pip install -r requirements-model.txt
